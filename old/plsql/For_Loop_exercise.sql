Lab Cursor For Loop1) Connect to the hr account in pdborcl.  Use the connection pdborcl_hr2) Create a temporary table load dataa. Create table temp(value number,odd_even varchar2(20);3) Create an anonoymous block to load odd and even data	DECLARE		x NUMBER := 100;BEGIN	 FOR i IN 1..10 LOOP		 IF MOD(i,2) = 0 THEN     -- i is even				INSERT INTO temp VALUES (i, x, 'i is even');			ELSE				INSERT INTO temp VALUES (i, x, 'i is odd');		END IF;	x := x + 100;	END LOOP;	COMMIT;	END;