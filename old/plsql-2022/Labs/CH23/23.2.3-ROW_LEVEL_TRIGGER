CREATE OR REPLACE TRIGGER before_insert_on_emp_TRG
  BEFORE
    INSERT 
  ON emps
  FOR EACH ROW
BEGIN
  INSERT INTO emp_DML_logs
        (table_name,user_name,operation,curr_date,new_emp_no)
  VALUES('EMPS',USER,'I',SYSDATE, :NEW.employee_id);
END;
/
