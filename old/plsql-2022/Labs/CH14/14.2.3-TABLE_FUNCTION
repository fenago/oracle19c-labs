CREATE TYPE emp_ids IS TABLE OF NUMBER
/
DECLARE
  v_emp      emp_ids;
BEGIN
  SELECT employee_id BULK COLLECT INTO v_emp 
  FROM   employees
  WHERE  ROWNUM <= 10;

  FOR r IN (SELECT * FROM TABLE(v_emp))
  LOOP
    dbms_output.put_line('-('||r.COLUMN_VALUE||')');
  END LOOP;
END;
/

