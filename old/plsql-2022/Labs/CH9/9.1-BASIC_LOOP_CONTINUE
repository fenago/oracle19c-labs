DECLARE
 i   	PLS_INTEGER;
 lower	PLS_INTEGER := 1;
 upper	PLS_INTEGER := 5;
BEGIN
   i := lower;
   LOOP
     EXIT WHEN i > upper;
     dbms_output.put_line('i='||i);
     i := i + 1;
     CONTINUE WHEN i = 4;
     dbms_output.put_line('Execute after CONTINUE');
   END LOOP;
END;
/
