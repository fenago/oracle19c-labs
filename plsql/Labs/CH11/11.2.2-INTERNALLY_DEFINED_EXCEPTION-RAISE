DECLARE
 v_employee_id   NUMBER; 
 v_email         VARCHAR2(50);
BEGIN
   SELECT employee_id, email INTO v_employee_id,v_email
   FROM   employees
   WHERE  employee_id = 150 
   FOR UPDATE NOWAIT;
END;
/
