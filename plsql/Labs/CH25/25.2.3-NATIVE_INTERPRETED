CREATE OR REPLACE PROCEDURE compilation_type 
AS
 v_grade   NUMBER;
 v_mark    VARCHAR2(100);
BEGIN
 FOR i IN 1..1000000 LOOP
  v_grade := DBMS_RANDOM.VALUE(0,100);
  v_mark  := mark(v_grade);
 END LOOP;
END;
/

